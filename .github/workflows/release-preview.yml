name: Release Preview

on:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  release_preview:
    uses: OpenVoiceOS/gh-automations/.github/workflows/release-preview.yml@dev
    with:
      package_name: 'HiveMind_Player_Protocol'
      version_file: 'hivemind_player_protocol/version.py'
